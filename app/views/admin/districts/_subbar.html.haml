%h2
  - case action_name 
  - when "index"
    = t('admin.districts.index.title')
  - when "new"
    = t('admin.districts.new.title')
  - when "create"
    = t('admin.districts.new.title')
  - when "edit"
    = t('admin.districts.edit.title')
  - when "update"
    = t('admin.districts.edit.title')
%uj.filter
  %li{:class => (action_name == "index" ? "current" : nil)}
    = link_to t('.index_button'), admin_districts_path
  %li{:class => ((action_name == "new" or action_name == "create") ? "current" : nil)}
    = link_to t('.new_button'), new_admin_district_path

