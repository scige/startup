.dashboard
  %ul
    %li{:class => (controller_name == "coupons" ? "current" : nil)}
      = link_to t('.my_coupons'), coupons_path
      %span

    %li{:class => (controller_name == "registrations" ? "current" : nil)}
      = link_to t('.my_account'), edit_user_registration_path(current_user)
      %span

